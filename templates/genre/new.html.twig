{% extends 'base.html.twig' %}

{% block title %}Ajouter Genre{% endblock %}

{% block body %}
<div class="container py-3">
    <h1>Ajouter Genre</h1>

    {{ include('genre/_form.html.twig') }}
    <br>
    <a href="{{ path('genre_index') }}" class="btn btn-primary"><i class="icon-refresh"></i> Retour</a>
</div>
{% endblock %}
